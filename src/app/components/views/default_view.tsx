import * as React from "react";

class DefaultView extends React.Component<any, any> {

    public render() {
        return (
            <div>Select a data retrieval library from the tabs above</div>
        );
    }
}

export default DefaultView;